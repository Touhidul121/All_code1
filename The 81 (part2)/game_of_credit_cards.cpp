#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    string s,t;
    cin>>s>>t;
    sort(s.begin(),s.end());
    sort(t.begin(),t.end());
    int cnt=0;
    for(int i=0;i<n;i++)
    {
      if(t[i]>=s[i])
            cnt++;
    }
    for(int i=0;i<n;i++)
    {
        if()
    }
}
